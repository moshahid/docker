const sidebar=document.querySelector('#sidebar');const menu=document.querySelector('#menu-button');const search=document.querySelector('#manual-search');const status=document.querySelector('#search-status');const content=document.querySelector('#manual-content');const topButton=document.querySelector('#back-to-top');
menu.addEventListener('click',()=>{const open=sidebar.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});
document.querySelectorAll('#TOC a').forEach(link=>link.addEventListener('click',()=>{sidebar.classList.remove('open');menu.setAttribute('aria-expanded','false')}));
document.querySelectorAll('blockquote, pre').forEach(block=>{const button=document.createElement('button');button.type='button';button.className='copy-command';button.textContent='Copy';button.addEventListener('click',async()=>{const text=block.innerText.replace(/^Copy\s*/, '').trim();try{await navigator.clipboard.writeText(text);button.textContent='Copied'}catch{const area=document.createElement('textarea');area.value=text;document.body.appendChild(area);area.select();document.execCommand('copy');area.remove();button.textContent='Copied'}setTimeout(()=>button.textContent='Copy',1400)});block.appendChild(button)});
search.addEventListener('input',()=>{const query=search.value.trim().toLowerCase();const sections=[...content.querySelectorAll('h1,h2')];document.querySelectorAll('#TOC li').forEach(item=>item.classList.toggle('search-hidden',query&&!item.textContent.toLowerCase().includes(query)));document.querySelectorAll('.search-match').forEach(node=>node.classList.remove('search-match'));if(!query){status.textContent='';return}const matches=sections.filter(node=>{const sectionText=[];let cursor=node;while(cursor&&sectionText.join(' ').length<5000){sectionText.push(cursor.textContent);cursor=cursor.nextElementSibling;if(cursor&&/H1|H2/.test(cursor.tagName))break}return sectionText.join(' ').toLowerCase().includes(query)});matches.forEach(node=>node.classList.add('search-match'));status.textContent=`${matches.length} matching section${matches.length===1?'':'s'}`});
window.addEventListener('scroll',()=>topButton.classList.toggle('visible',window.scrollY>700),{passive:true});topButton.addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));
